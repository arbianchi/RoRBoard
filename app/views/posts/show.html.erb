
<h2><%= @post.title%></h2>


<h3><%= @post.content%></h3>


<table class="table table-hover table-striped table-condensed">
  <thead>
    <tr>
      <th><h4>Comments: <span class="badge"><%= @post.comments.count %></span></h4></th>
      <th>Score</th>
      <th>Author</th>
    </tr>
  </thead>
  <tbody>
    <% @post.comments.each do |comment| %>
      <tr>
        <td><%= comment.content %></td>
        <%#       <%= link_to comment_vote_path(comment, value: 1), method: :post do %1> %> 
        <%#         <span class="glyphicon glyphicon-upload up-btn"></span> %>
        <%#       <%end%>
      <%#       <%= link_to comment_vote_path(comment, value: -1), method: :post do %1> %> 
      <%#         <span class="glyphicon glyphicon-download"></span> %>
      <%#       <%end%> 
    <td>
      <a href="#" class="glyphicon glyphicon-upload up-btn" data-comment-id = <%= comment.id %>>
      <a href="#" class="glyphicon glyphicon-download down-btn" data-comment-id = <%= comment.id %>>
    </td>
    <%# <td><%= Vote.where(comment_id: comment.id).pluck(:value).flatten.reduce(:+) %1></td> %>
        <td class="total-votes" data-comment-id = <%= comment.id %>></td>
      <td><%= User.find(comment.user_id).email %></td>
  </tr>
<% end %>
  </tbody>
</table>

<h4>Make a comment</h4>
<%= link_to "Comment", new_post_comment_path(@post),class: "btn btn-default"%>
